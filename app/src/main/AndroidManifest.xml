<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vladiknt.blackgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Statistics"></activity>
        <activity android:name=".Menu" />
        <activity android:name=".Themes" />
        <activity android:name=".Settings" />
        <activity android:name=".Rules" />
        <activity android:name=".MenuRoulette" />
        <activity android:name=".DeveloperPage" />
        <activity android:name=".Changes" />
        <activity android:name=".MainActivityRoulette" />
        <activity android:name=".StartPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>